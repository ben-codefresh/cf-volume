version: '1.0'

steps:
    prepare:
        image: ubuntu:latest
        commands:
            - echo ls volume
            - base -c "echo ben > /coedfresh/volume/ben.txt"
            - bash -c "ls /coedfresh/volume"