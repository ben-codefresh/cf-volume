version: '1.0'

steps:
    prepare:
        image: ubuntu:latest
        working_directory: ${{main_clone}}
        commands:
            - echo ls current dir
            - ls
            - echo ls volume
            - bash -c "echo ben >  '${{CF_VOLUME}}/ben.txt'" #/codefresh/volume
            - bash -c "ls '${{CF_VOLUME}}'"