version: '1.0'

steps:
    prepare:
        image: ubuntu:latest
        commands:
            - echo ls volume
            - base -c "echo ben > ${{CF_VOLUME}}/ben.txt"
            - bash -c "ls ${{CF_VOLUME}}"